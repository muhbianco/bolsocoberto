# Testes do editor (FastAPI + worker de pauta).
when:
  - event: [push, pull_request, manual]

steps:
  tests:
    image: python:3.12-slim
    environment:
      PIP_DISABLE_PIP_VERSION_CHECK: "1"
    commands:
      - pip install --quiet -r requirements-dev.txt
      - python -m pytest -q
